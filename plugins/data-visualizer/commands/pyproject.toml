[project]
name = "data-visualizer"
version = "0.1.0"
description = "Visualize open data from URLs using Gradio gr.HTML with Plotly charts"
requires-python = ">=3.13"
dependencies = [
    "gradio>=5.0.0",
]
