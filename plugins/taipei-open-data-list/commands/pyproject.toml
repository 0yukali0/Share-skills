[project]
name = "taipei-open-data-list"
version = "0.1.0"
description = "Query Taipei Open Data dataset list, optionally filtered by category"
requires-python = ">=3.13"
dependencies = [
    "asyncio>=4.0.0",
    "flyte>=2.0.9",
    "httpx>=0.27.0",
]
