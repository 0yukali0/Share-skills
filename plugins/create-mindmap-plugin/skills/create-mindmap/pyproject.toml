[project]
name = "create-mindmap"
version = "0.1.0"
description = "Generate mindmap from text input as Mermaid syntax and PNG image"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flyte>=2.0.9",
    "pytest>=8.0.0",
]
